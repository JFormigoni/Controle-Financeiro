/**
 * Função pura trivial usada apenas para validar a infraestrutura de testes
 * (resolução do alias `@/*` e execução do harness). Será substituída pela
 * lógica de domínio real nas tarefas subsequentes.
 */
export function add(a: number, b: number): number {
  return a + b;
}
